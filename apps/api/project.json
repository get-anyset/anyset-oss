{
  "name": "api",
  "root": "apps/api",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/api && uv sync && uv run python main.py"
      }
    }
  }
} 